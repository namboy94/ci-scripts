stages:
  - mirror
  - test

github_mirror:
  stage: mirror
  tags:
    - python
  script:
    - echo $GITHUB_USERNAME
    - python src/github-mirror.py master

stylecheck:
  stage: test
  tags:
    - pycodestyle
  script:
    - pycodestyle .
